apiVersion: batch/v1
kind: Job
metadata:
  generateName: slow-job-
  namespace: default
spec:
  # Automatically delete job after completion
  ttlSecondsAfterFinished: 300
  template:
    spec:
      containers:
      - name: sleeper
        image: busybox:latest
        command: ["sh", "-c", "echo 'Starting slow job'; sleep 30; echo 'Job complete'"]
      restartPolicy: Never
  backoffLimit: 0
