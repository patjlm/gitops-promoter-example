apiVersion: v1
kind: Service
metadata:
  labels:
    app: app-b
    env: prod-1
  name: app-b
  namespace: app-b-prod-1
spec:
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: app-b
    env: prod-1
  type: ClusterIP
