apiVersion: v1
kind: Service
metadata:
  labels:
    app: app-b
    env: development
  name: app-b
  namespace: app-b-development
spec:
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: app-b
    env: development
  type: ClusterIP
