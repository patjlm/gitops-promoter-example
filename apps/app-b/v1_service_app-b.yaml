apiVersion: v1
kind: Service
metadata:
  labels:
    app: app-b
    env: stage
  name: app-b
  namespace: app-b-stage
spec:
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: app-b
    env: stage
  type: ClusterIP
