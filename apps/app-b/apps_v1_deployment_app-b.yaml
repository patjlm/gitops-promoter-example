apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    test-ci-fail: "true"
  labels:
    app: app-b
    env: integration
  name: app-b
  namespace: app-b-integration
spec:
  replicas: 2
  selector:
    matchLabels:
      app: app-b
      env: integration
  template:
    metadata:
      labels:
        app: app-b
        env: integration
    spec:
      containers:
      - image: kennethreitz/httpbin:v1.0
        name: app-b
        ports:
        - containerPort: 80
        resources:
          limits:
            cpu: 200m
            memory: 192Mi
          requests:
            cpu: 50m
            memory: 64Mi
