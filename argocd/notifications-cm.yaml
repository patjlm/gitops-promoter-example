apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-notifications-cm
  namespace: argocd
data:
  # GitHub service configuration using GitHub App
  service.github: |
    appID: $github-app-id
    installationID: $github-installation-id
    privateKey: $github-app-private-key

  # Single unified template for all apps (including all-apps)
  # GitHub status shows: label + message (no separate description field)
  template.github-status: |
    message: |
      phase={{.app.status.operationState.phase | default "none"}} sync={{.app.status.sync.status}} health={{.app.status.health.status}}
    github:
      repoURLPath: "{{.app.spec.source.repoURL}}"
      revisionPath: "{{.app.status.sync.revision}}"
      status:
        state: "{{if or (eq .app.status.operationState.phase \"Running\") (eq .app.status.health.status \"Progressing\") (eq .app.status.health.status \"Missing\") (eq .app.status.sync.status \"OutOfSync\")}}pending{{else if and (eq .app.status.sync.status \"Synced\") (eq .app.status.health.status \"Healthy\")}}success{{else}}failure{{end}}"
        label: "ArgoCD/{{.app.metadata.name}}"
        targetURL: "https://argocd.example.com/applications/{{.app.metadata.name}}"

  # Single trigger for all apps
  # Note: ArgoCD notifications has no built-in delay mechanism
  # The oncePer deduplication + 30s reconciliation provides natural debouncing
  trigger.on-sync-status-change: |
    - when: app.status.operationState != nil
      oncePer: app.status.sync.revision + "-" + app.status.operationState.phase
      send: [github-status]
    - when: app.status.sync.status == "Synced" && app.status.health.status == "Healthy"
      oncePer: app.status.sync.revision
      send: [github-status]
